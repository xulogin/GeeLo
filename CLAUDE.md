# GeeLo

**完整说明在同目录的 `AGENTS.md`，请先读那一份再动手。**

@AGENTS.md

（如果上面这一行没有被自动加载，就**直接打开读** `AGENTS.md`。
那份文件不到 200 行，写清了两个功能是什么、八条铁律、常用命令和目录地图，读完就能开工。）

---

先记住最要紧的两条，万一没读到 `AGENTS.md` 也别犯错：

1. **写完 GEE 脚本必须实跑验证再交付**，不许只静态审读：
   `node "<GeeLo>\测试台\跑GEE.js" --timeout 900 "<脚本.js>"`
2. **「跑通」不等于「对」**——GEE 是惰性计算，零报错也可能结果全错。
   跑完还要看数值合不合理、量级对不对、符号对不对。

**给人看的入口**在 `README.md`。
